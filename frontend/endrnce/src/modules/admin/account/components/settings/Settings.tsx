import React, { FC } from "react";
import { ProfileDetails } from "@modules/admin/account/components/settings/cards/ProfileDetails";

const Settings: FC = () => {
    return (
        <>
            <ProfileDetails />
        </>
    );
};

export { Settings };
